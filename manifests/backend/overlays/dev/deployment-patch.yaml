apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: app
spec:
  template:
    spec:
      containers:
        - name: backend
          env:
            - name: DEBUG
              value: "true"
